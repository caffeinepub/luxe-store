{
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — fixed the two TypeScript errors on lines 145 and 179 by replacing unsafe `identity?.getPrincipal()!` non-null assertions with safe `Principal.anonymous()` fallbacks in the cart and wishlist query functions (which are guarded by `enabled: !!identity` anyway). Also removed unused `ReviewId` import.",
  "frontend/src/components/CartItemCard.tsx": "Cart item row component displaying product image, name, variant info, quantity picker, and price — replaced deprecated `flex-shrink-0` with `shrink-0` on two elements.",
  "frontend/src/components/CategoryCard.tsx": "Clickable category card with image overlay and hover effects; uses `productCount` prop (not `count`) for item count display.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with logo, nav links, search bar, cart badge, wishlist, profile, admin, and login/logout — replaced `flex-shrink-0` with `shrink-0` and removed unused `useRouter`, `X`, and `Badge` imports.",
  "frontend/src/components/OrderStatusTimeline.tsx": "Visual step-by-step order status timeline using `String(h.status)` comparison (not `.__kind__`) since OrderStatus is an enum, with gold indicators and timestamps.",
  "frontend/src/components/ProductFormModal.tsx": "Admin dialog for creating or editing products with all fields including variants — replaced `flex-shrink-0` with `shrink-0`, removed unused `ProductVariant` import, and fixed the `catch (err)` to `catch` to avoid unused variable warning.",
  "frontend/src/components/ProductImageGallery.tsx": "Swipeable product image gallery with main view and thumbnail strip — replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/components/ProductManagementTable.tsx": "Admin table listing all products with edit/delete actions and add product button — replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/App.tsx": "Main app with TanStack Router; order tracking route is correctly defined as `/orders/$orderId` (not `/order-tracking/$orderId`) to match all navigation calls.",
  "frontend/src/pages/Home.tsx": "Home page with hero carousel (no props), category cards using `productCount` prop, featured products grid, promo banner, and new arrivals section.",
  "frontend/src/pages/ProductListing.tsx": "Product catalog page that derives `categories` and `subCategories` from loaded products and passes them to FilterPanel; fixes SortDropdown type and removes invalid `onClose` prop.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page with image gallery, size/color variant selectors, quantity picker, add-to-cart and wishlist CTAs, and customer reviews section — all wired to backend.",
  "frontend/src/pages/Cart.tsx": "Shopping cart page showing cart items with quantity controls and remove functionality, order summary with totals, and checkout navigation — requires authentication.",
  "frontend/src/pages/Wishlist.tsx": "Wishlist page displaying saved products in a grid with move-to-cart and remove actions — requires authentication and shows empty/login states.",
  "frontend/src/pages/Checkout.tsx": "Multi-step checkout wizard with `paymentMethod` defaulting to `cashOnDelivery` (never null) so PaymentMethodSelector always receives a valid value.",
  "frontend/src/pages/OrderConfirmation.tsx": "Order confirmation page; navigation to order tracking now uses the correct route `/orders/$orderId` instead of the non-existent `/order-tracking/$orderId`.",
  "frontend/src/pages/OrderHistory.tsx": "Order history page; navigation to individual order tracking now uses the correct route `/orders/$orderId` and uses `String(order.status)` for status lookups.",
  "frontend/src/pages/OrderTracking.tsx": "Order tracking page; `useParams` now uses the correct route path `/orders/$orderId` matching the router definition, with `String(order.status)` for status display.",
  "frontend/src/pages/Profile.tsx": "User profile page with editable name, email, and phone fields — pre-populated from backend and saved via saveCallerUserProfile mutation.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel page with Products and Orders tabs — checks admin status via backend, shows AccessDeniedScreen for non-admins.",
  "frontend/src/pages/SearchResults.tsx": "Search results page that derives `categories` and `subCategories` from search results and passes them to FilterPanel; fixes SortDropdown type and removes invalid `onClose` prop.",
  "frontend/src/components/OrderManagementTable.tsx": "Admin order management table showing all orders with status badges and inline status update dropdowns wired to updateOrderStatus backend mutation.",
  "frontend/src/utils/urlParams.ts": "URL parameter utility functions including `getSecretParameter` (required by useActor.ts) that reads from URL query params with sessionStorage fallback, plus price formatting and timestamp helpers.",
  "frontend/src/components/ProductCard.tsx": "Product card with image, discount badge, wishlist toggle, and navigation; now exports `ProductCardSkeleton` as a named export for use in loading states across pages.",
  "frontend/src/components/HeroCarousel.tsx": "Auto-advancing hero banner carousel with hardcoded slides (no props accepted), manual navigation arrows, and dot indicators using generated banner images.",
  "frontend/src/components/FilterPanel.tsx": "Filter panel with category, subcategory, and price range controls; `categories` and `subCategories` are now optional (default empty arrays) and accepts an optional `onClose` callback.",
  "frontend/src/components/SortDropdown.tsx": "Sort dropdown with four sort options; `value` prop accepts `string` type for flexibility with page state while `onChange` still emits typed `SortOption`.",
  "frontend/src/components/PaymentMethodSelector.tsx": "Payment method selector for UPI, credit/debit card, wallet, and cash on delivery; `value` prop is `PaymentMethod` (never null) — callers must provide a default value."
}